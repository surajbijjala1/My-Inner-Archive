# Used by `npm run build` (production mode) — the build that Capacitor wraps
# into the APK and that Vercel serves (Vercel's dashboard env var overrides this).
# Public URL, not a secret.
VITE_API_URL=https://my-inner-archive-api.onrender.com
